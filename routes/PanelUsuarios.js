//Código de Aarón el que lo copie es puto.
const User = require("../models/Usuarios.js"); module.exports= async(req,res)=>{ var page = req.query.page; if (page === undefined){ const usuarios = await User.paginate({},{page:1,limit:30}); const FiltroPaginado= false; const Filtro = "ninguno"; console.log(usuarios); res.render('PanelUsuarios',{Logeado,role,usuarios,FiltroPaginado,Filtro}); }else if (page != undefined){ const usuarios = await User.paginate({},{page,limit:30}); const FiltroPaginado= false; const Filtro = "ninguno"; res.render('PanelUsuarios',{Logeado,role,usuarios,FiltroPaginado,Filtro}); } }
